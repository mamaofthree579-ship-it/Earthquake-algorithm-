# IHRAS v1.0 — Integrated Harmonic Risk & Awareness System

Prototype open-source project to ingest terrestrial & celestial data, harmonize signals,
run simple correlation models and present alerts/visualizations via Streamlit.

## Quick start

1. Create virtualenv with Python 3.10+:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
